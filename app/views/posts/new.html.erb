<%= form_with(model: @post, local: true) do |f|%>
  <%= render "shared/error_messages", model: f.object %>
  <%= f.text_area :title, placeholder: "タイトルを入力" %>
  <%= f.text_field :description, placeholder: "説明を入力" %>
  <%= f.submit "送信" %>
<% end %>